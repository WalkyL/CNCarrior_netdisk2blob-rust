interface:
  display_name: "Carrier Cloud Blob Gateway"
  short_description: "Explain safe S3 field mapping and diagnose provider and replication status."
  default_prompt: "Use $carrier-cloud-blob-gateway to explain bucket/prefix versus region semantics for S3 integrations, then check provider health, replication state, and safe fallback readiness before risky operations."
